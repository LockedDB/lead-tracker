# Pendientes

- Crear lead/curro en blanco desde la UI (botón "+ Nuevo")
- Historial de generaciones en el popup (falta endpoint `GET /api/generations`)
- Definir `STYLE_RULES` / cablear el humanizer para la voz de marca en la generación
- Board kanban como vista alternativa (toggle)
